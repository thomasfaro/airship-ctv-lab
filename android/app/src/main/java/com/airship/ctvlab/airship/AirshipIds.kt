package com.airship.ctvlab.airship

object AirshipIds {
    /** Screen name used by Airship Automation when the home catalogue is visible. */
    const val HOME_SCREEN = "home"

    /** Embedded Scene ID for the home catalog banner. Must match the dashboard view style. */
    const val HOME_BANNER = "home_banner"
}
